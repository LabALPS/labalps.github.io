<nav class="diss-nav">
  <a class="diss-brand" href="/events/DiSS2027/">DiSS 2027</a>
  <div class="diss-links">
    <a
      href="/events/DiSS2027/Callforpapers/"
      {% if page.url contains 'Callforpapers' %}
        class="active"
      {% endif %}
      >Call for Papers</a
    >
    <a
      href="/events/DiSS2027/Program/"
      {% if page.url contains 'Program' %}
        class="active"
      {% endif %}
      >Program</a
    >
    <a
      href="/events/DiSS2027/Committees/"
      {% if page.url contains 'Committees' %}
        class="active"
      {% endif %}
      >Committees</a
    >
    <a
      href="/events/DiSS2027/Registration/"
      {% if page.url contains 'Registration' %}
        class="active"
      {% endif %}
      >Registration</a
    >
    <a
      href="/events/DiSS2027/Venue/"
      {% if page.url contains 'Venue' %}
        class="active"
      {% endif %}
      >Venue</a
    >
    <a
      href="/events/DiSS2027/Contact/"
      {% if page.url contains 'Contact' %}
        class="active"
      {% endif %}
      >Contact</a
    >
  </div>
</nav>

<style>
  .diss-nav {
    position: sticky;
    top: 0;
    z-index: 100;
    display: flex;
    justify-content: space-between;
    align-items: center;
    flex-wrap: wrap;
    gap: 0.5rem 2rem;
    padding: 0.8rem 0;
    margin-bottom: 2rem;
    background: var(--global-bg-color);
    border-bottom: 1px solid var(--global-divider-color);
  }
  .diss-brand {
    font-size: 1.45rem;
    font-weight: 300;
    color: var(--global-text-color);
    text-decoration: none;
  }
  .diss-links {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem 1.5rem;
  }
  .diss-links a {
    color: var(--global-text-color);
    text-decoration: none;
    font-size: 1.05rem;
  }
  .diss-links a:hover {
    color: var(--global-hover-color);
  }
  .diss-links a.active {
    color: var(--global-theme-color);
    font-weight: bolder;
  }

  /* nasconde la navbar principale del sito nelle pagine DiSS */
  body > header {
    display: none;
  }
</style>
